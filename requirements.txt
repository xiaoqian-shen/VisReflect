# VisReflect standalone evaluation pipeline dependencies.
# This set is verified to install cleanly from public PyPI (Python 3.12, CUDA 12.4) and to
# run the eval end-to-end. The reproduction-critical packages (torch, transformers,
# qwen-vl-utils, decord) are pinned to the versions the checkpoints were validated against;
# the rest are pinned to versions that co-install with them.
#   pip install -r requirements.txt

# --- Core model / inference ---------------------------
torch==2.6.0
torchvision==0.21.0
transformers==4.54.0
flash-attn==2.7.4.post1
# Qwen2.5-VL vision preprocessing. 0.0.14 is REQUIRED: 0.0.8 lacks the
# image_patch_size / return_video_kwargs kwargs the eval passes (-> TypeError on images).
qwen-vl-utils==0.0.14

# --- Runtime deps (versions that co-install with transformers 4.54.0) ---------
accelerate==1.14.0
datasets==5.0.0
huggingface-hub==0.36.2       # transformers 4.54 needs >=0.34; the 0.30.x freeze pin conflicts
numpy==2.5.1
pandas==3.0.3
pyarrow==24.0.0
pillow==12.3.0
tqdm==4.68.3
ujson==5.13.0                 # training data loading (src/dataset)

# --- Video decoding (video benchmarks only) -----------------------------------
decord==0.6.0
av==18.0.0
